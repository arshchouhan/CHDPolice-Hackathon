# =======================
# Server Configuration
# =======================
PORT=3000
HOST=0.0.0.0
NODE_ENV=development
NODE_OPTIONS=--max-old-space-size=768

# =======================
# Database Configuration
# =======================
# MongoDB Connection String (Production)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/email-detection?retryWrites=true&w=majority

# =======================
# Security & Authentication
# =======================
# JSON Web Token Secret (Generate a strong secret in production)
JWT_SECRET=your_64_character_long_jwt_secret_key_change_this_in_production
JWT_EXPIRES_IN=7d
JWT_COOKIE_EXPIRES=7

# =======================
# Google OAuth Configuration
# =======================
GOOGLE_CLIENT_ID=your_google_oauth_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_oauth_client_secret

# OAuth Callback URLs (Update with your production domain)
GOOGLE_CALLBACK_URL_DEV=http://localhost:3000/api/auth/google/callback
GOOGLE_CALLBACK_URL_PROD=https://your-render-app.onrender.com/api/auth/google/callback

# =======================
# AI & External Services
# =======================
# Google Gemini API Key
GEMINI_API_KEY=your_gemini_api_key_here

# VirusTotal API Key (Optional)
VIRUSTOTAL_API_KEY=your_virustotal_api_key

# =======================
# CORS & Security Headers
# =======================
# Allowed origins (comma-separated for multiple origins)
CORS_ORIGIN_DEV=http://localhost:3000,http://localhost:3001
CORS_ORIGIN_PROD=https://your-netlify-app.netlify.app

# Content Security Policy (CSP) - Adjust as needed
CSP_DIRECTIVES="default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline';"

# =======================
# Rate Limiting
# =======================
RATE_LIMIT_WINDOW_MS=15*60*1000  # 15 minutes
RATE_LIMIT_MAX=100  # Limit each IP to 100 requests per windowMs

# =======================
# Logging
# =======================
LOG_LEVEL=info  # error, warn, info, debug
LOG_FORMAT=combined  # combined, common, dev, short, tiny

# =======================
# Session Configuration
# =======================
SESSION_SECRET=your_session_secret_change_this
SESSION_COOKIE_MAX_AGE=24*60*60*1000  # 24 hours

# =======================
# Email Configuration (if applicable)
# =======================
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=your_smtp_username
SMTP_PASSWORD=your_smtp_password
SMTP_FROM=noreply@example.com
